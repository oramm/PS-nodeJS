node_modules/
build/
.env
.env.development
.env.production
.env.local
Tests/

# Tokeny OAuth zbierane do migracji GD (poświadczenia - NIE commitować!)
tokens.json

# Artefakty operacyjne migracji Google Drive (raporty, manifesty, snapshoty, logi)
gd-out/
gd-*.csv
gd-*.json
gd-*.jsonl
gd-*.txt
gd-*.log
snap-*.jsonl
bak-*.jsonl
*.log

# Test coverage (generowane lokalnie - NIE commitować!)
coverage/
*.lcov

# Jest cache
.jest/
tmp/dev-runtime/

# Claude Code worktrees (tworzone automatycznie, nie commitować)
.claude/worktrees/

# Playwright MCP logs (tymczasowe artefakty lokalne)
.playwright-mcp/

# Yarn diagnostics log (generowany lokalnie przy failed run)
yarn-error.log

# Local DB migration check state (private, regenerated by skill)
tmp/db-migration-state/

# Zrzuty struktury arkuszy zaliczek/poczty (zawieraja dane firmowe: adresatow,
# tresci listow, numery faktur, kwoty i adresy e-mail - NIE commitowac!)
# Regenerowane lokalnie przez: yarn pettycash:inspect
tmp/pettycash-sheet-structure*.json
